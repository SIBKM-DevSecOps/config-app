eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_SERVER_ADDRESS:http://service-registry:8762/eureka}

okta:
  oauth2:
    issuer: https://dev-47911663.okta.com/oauth2/default
    audience: api://default